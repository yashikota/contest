#include <bits/stdc++.h>

#define ll long long

int main() {
    int n;
    std::cin >> n;
    std::vector<int> q(n), a(n), b(n);

    for (int i = 0; i < n; i++) {
        std::cin >> q.at(i) >> a.at(i) >> b.at(i);
    }

    return 0;
}
